CREATE TABLE promotion_coupon (
                                  coupon_code VARCHAR(64) PRIMARY KEY,
                                  discount_info TEXT,
                                  valid BOOLEAN
);